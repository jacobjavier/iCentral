const express = require('express');
const router = express.Router();

router.get('/getservices/:userId', (req, res) => {
    // Lógica para obtener servicios en tu backend Node.js
    // Deberías obtener los servicios del usuario con el userId proporcionado y devolver la respuesta.
});

module.exports = router;
